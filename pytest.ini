[pytest]
pythonpath = src
testpaths = tests
markers =
    slow: synthetic-dataset or multi-step pipeline tests that take noticeably longer than the unit-level checks
    model: optional model/checkpoint/backend tests that require heavyweight dependencies or downloads
